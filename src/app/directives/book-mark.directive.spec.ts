import { BookMarkDirective } from './book-mark.directive';

describe('BookMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new BookMarkDirective();
    expect(directive).toBeTruthy();
  });
});
